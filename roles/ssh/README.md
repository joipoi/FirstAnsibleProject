SSH
=========
Sets up ssh, allows it on the network and does some safety features

Role Variables
--------------
```yaml
allowed_ssh_networks: 
```
Dependencies
------------
This role is expected to be used with the other roles in this github repo.
It might not work as a standalone role